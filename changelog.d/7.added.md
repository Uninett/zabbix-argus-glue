Add a `[filter]` section with a `hostgroups` allow-list to restrict which Zabbix problems are synced to Argus by host group, applied consistently across the reconciliation and webhook paths. Host-group membership is now also emitted as `hostgroup` tags on incidents.
